## 1. Project Data Update

- [x] 1.1 Update `projects` array in `app/page.tsx` with detailed information for `NeoMetro`, `RapidExpres`, `SISTEMA_AEROPUERTO`, `AMB-exam`, and `MetaNav`.
- [x] 1.2 Verify technology tags, summaries, and GitHub repository links for all 5 projects.

## 2. Layout & UI Polish

- [x] 2.1 Verify card grid layout and responsiveness in `app/page.tsx` for 5 items.
- [x] 2.2 Ensure hover micro-animations and button styles function properly across all cards.

## 3. Build & Verification

- [x] 3.1 Validate TypeScript types and run Next.js build (`npm run build`).
