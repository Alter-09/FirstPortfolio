## 1. Theme State & Control Setup

- [x] 1.1 Implement theme state (`theme` = `'light'` | `'dark'`) with `localStorage` persistence and `prefers-color-scheme` fallback in `app/page.tsx`.
- [x] 1.2 Add theme toggle button (Sun/Moon icons) with neubrutalist styling to header navigation.

## 2. Neubrutalist Dark Palette Styling

- [x] 2.1 Refine dark theme CSS tokens and classes in `app/globals.css` and `app/page.tsx` for dark canvas (`#141413`) and dark card surfaces (`#1e1d1a`).
- [x] 2.2 Ensure high contrast text colors, borders, and dot-grid styling for dark mode across all sections.
- [x] 2.3 Verify accent colors (Red, Yellow, Teal) maintain punchy visibility on dark surfaces.

## 3. Build & Verification

- [x] 3.1 Verify theme switching, persistence across reloads, and mobile/desktop responsiveness.
- [x] 3.2 Validate TypeScript types and run Next.js build (`npm run build`).
