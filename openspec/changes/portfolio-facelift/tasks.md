## 1. Global Setup and Design Tokens

- [x] 1.1 Update `index.css` (or equivalent global styles) to include dark mode CSS variables (e.g., `--bg-color: #1e2327`, `--text-color: #ffffff`, `--accent-color: #c30f1f`).
- [x] 1.2 Import a modern sans-serif font (like Inter or Roboto) and apply it to the `body` tag.

## 2. Layout Restructuring (App Shell)

- [x] 2.1 Refactor the main container layout into an `app-shell` pattern.
- [x] 2.2 Create/Update the Sidebar component to act as persistent navigation on desktop.
- [x] 2.3 Implement responsive behavior for the navigation (e.g., topbar or collapsible menu for mobile screens).

## 3. Styling Components and Interactive Elements

- [x] 3.1 Update all buttons and call-to-action links to use the new red accent color.
- [x] 3.2 Add hover effects and micro-animations to navigation links, project cards, and buttons for a premium feel.
- [x] 3.3 Ensure sufficient contrast between text elements and the new dark background.

## 4. Final Review and Polish

- [x] 4.1 Test the new design across different viewports (mobile, tablet, desktop).
- [x] 4.2 Verify that all portfolio sections (About, Projects, Contact) render correctly within the new app-shell.
- [x] 4.3 Clean up any unused legacy CSS classes or colors.
